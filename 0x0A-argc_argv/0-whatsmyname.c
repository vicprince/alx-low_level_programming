#include "main.h"
/**
 * main - prints name
 * @argc: hold the number of arguments
 * @argv: array pointer where th arguments are stored
 *
 * Return: Always 0 Success
 */
int main(int __attribute__((unused)) argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
